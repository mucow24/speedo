# raw page snapshots: disposable debug/reparse cache -- everything of value
# is extracted into the committed JSONL datasets at ingest
data/raw/
# generated maps (reproducible from the datasets in seconds)
out/
__pycache__/
*.egg-info/
